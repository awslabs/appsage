When the graph is way too big, you need to filter it down to a manageable size.
This allows you to filter the graph data based on node types and/or edge types.
The specified node and edge types are included in the resulting graph, while all other types are excluded.